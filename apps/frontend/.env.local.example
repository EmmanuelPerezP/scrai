# Origin of the backend API as seen from the browser.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Optional: origin used by server components / SSR (e.g. the docker service name).
# Falls back to NEXT_PUBLIC_API_BASE_URL when unset.
API_BASE_URL_INTERNAL=http://localhost:4000
